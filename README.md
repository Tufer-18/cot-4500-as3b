This code is designed to perform Gaussian elimination, LU factorization, and checks for diagonal dominance and positive definite on various matrices.
The top portion of the code defines the functions, while the bottom half is where the matrices are made and the functions are called.
To run the code, copy and paste the code into a compiler, enter your own values (and change the number of rows) in the areas where comments specify, and let it work.
